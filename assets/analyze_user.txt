请分析以下 Minecraft 日志，文件名：`{{source_name}}`，提取策略：`{{strategy}}`。
请严格按系统要求的结构输出 Markdown，标题只允许使用以下 4 个且顺序固定：
- 核心问题
- 关键证据
- 解决步骤
- 补充确认

要求：
- 优先给“可执行步骤”，不要空泛建议。
- 证据尽量引用日志中的关键词/异常名/模组名，并用 E1/E2 编号。
- 每一步都要标注其依据的证据编号（E?）。
- 关键证据最多 4 条，解决步骤最多 4 步；不要输出表格、代码块、额外标题或结构外说明。
- 不要回显完整路径、IP、邮箱、token 或大段堆栈；只写短证据摘要。
- 不要复述这些要求，不要追加“免责声明/注意事项/总结/更多建议”等段落。
- 如果证据不足以确定根因，核心问题必须写 `UNCERTAIN`，并在“补充确认”里给最小补充材料清单。
- 解决步骤每一步必须包含：可逆性提醒、风险等级（低/中/高）、端区分（Client/Server/Both），并按低→高风险排序。
- 如果日志更像模组冲突/兼容性问题，优先给“检查并调整配置文件、功能开关、世界生成/配方相关设置、客户端与服务端配置一致性、版本对齐”的步骤；日志不足以定位具体配置项时，不要编造配置名。
- 如果日志已明确指向 1-5 个嫌疑模组，且问题可能与崩溃栈、mixin、依赖缺失/冲突、渲染、Java 或加载器兼容有关，可调用 `check_mod_fix_status` 查询兼容新版 changelog；不能因为有新版就建议更新，只有工具返回 fixed_likely/recommend_update 时才可明确建议更新。
- “删除/卸载模组”只能作为最后的隔离验证手段；优先使用“临时禁用/移出模组验证”的表述，且只有在前面低风险步骤无效或日志明确显示硬性不兼容时才可提及。
- 如果你调用过搜索工具，最后必须追加“网络搜索仅供参考”的提示行。

{{content}}
